import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faLocationDot as faLocation } from '@fortawesome/free-solid-svg-icons';

export const LocationIcon = (props) => <FontAwesomeIcon icon={faLocation} {...props} />;
